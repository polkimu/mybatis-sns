info:
  name: RefreshToken
  type: http
  seq: 8

http:
  method: POST
  url: http://localhost:8080/api/v1/auth/refresh
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
