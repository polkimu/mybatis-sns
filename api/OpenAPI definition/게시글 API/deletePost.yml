info:
  name: deletePost
  type: http
  seq: 3
  tags:
    - 게시글 API

http:
  method: DELETE
  url: "{{baseUrl}}/api/v1/posts/:id"
  headers:
    - name: X-Member-Id
      value: ""
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/posts/:id"
      method: DELETE
      headers:
        - name: X-Member-Id
          value: ""
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
