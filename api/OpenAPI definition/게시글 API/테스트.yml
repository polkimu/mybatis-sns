info:
  name: 테스트
  type: http
  seq: 6

http:
  method: GET
  url: /api/v1/members/me
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
