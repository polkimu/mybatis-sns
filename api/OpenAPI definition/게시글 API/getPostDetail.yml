info:
  name: getPostDetail
  type: http
  seq: 1
  tags:
    - 게시글 API

http:
  method: GET
  url: "{{baseUrl}}/api/v1/posts/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/posts/:id"
      method: GET
      params:
        - name: id
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "id": 0,
            "memberId": 0,
            "content": "",
            "imageUrl": "",
            "likeCount": 0,
            "createdAt": "",
            "updatedAt": ""
          }
