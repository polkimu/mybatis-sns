info:
  name: JWT인증
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/api/v1/posts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
